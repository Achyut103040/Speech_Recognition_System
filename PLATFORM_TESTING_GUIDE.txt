╔════════════════════════════════════════════════════════════════════════════╗
║      PLATFORM TESTING & DEPLOYMENT - STEP-BY-STEP GUIDE                    ║
╚════════════════════════════════════════════════════════════════════════════╝

🎯 GOAL: Deploy Speech Recognition app to Windows, Android, and Web

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🖥️  PART 1: WINDOWS TESTING & DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 1A: Basic Testing
───────────────────────

1. Open PowerShell in d:\Speech_Recognition

2. Activate virtual environment:
   .venv\Scripts\activate

3. Run the app:
   python main.py

4. Verify on screen:
   ✓ App window appears
   ✓ Title: "Speech Recognition App"
   ✓ 6 buttons visible:
     - Record (top-left)
     - Upload (top-right)
     - Visualize (middle-left)
     - Transcribe (middle-right)
     - Settings (bottom-left)
     - Exit (bottom-right)

5. Click each button and verify:
   ✓ Record      → Opens recording screen, shows duration input
   ✓ Upload      → Opens file selection dialog
   ✓ Visualize   → Shows visualization options
   ✓ Transcribe  → Opens transcription interface
   ✓ Settings    → Opens settings screen
   ✓ Exit        → Closes app cleanly

6. Test Back button on each screen:
   ✓ Returns to home screen
   ✓ No errors


PHASE 1B: Audio Testing
──────────────────────

1. Click "Record" button

2. Enter duration: 5

3. Click "Start Recording"

4. Speak: "Hello, this is a test"

5. Verify:
   ✓ Recording indicator shows
   ✓ Audio file saved to recordings/ folder
   ✓ "Recording complete" message appears

6. Check recordings/ folder:
   dir recordings\
   ✓ .wav file created with timestamp


PHASE 1C: Vosk Model Setup
──────────────────────────

1. Check if Vosk model exists:
   cd models
   dir
   
   If empty or not found:

2. Download model:
   cd ..
   python model_downloader.py

3. Wait for download (first time only):
   Model size: ~50MB
   Download time: 2-5 minutes

4. Verify model downloaded:
   dir models\
   ✓ Folder contains model files


PHASE 1D: Transcription Testing
───────────────────────────────

1. Click "Transcribe" button

2. Select audio file from recordings/ folder

3. Click "Transcribe"

4. Wait for processing:
   ✓ Progress indicator shows
   ✓ Processing time: 5-30 seconds (depends on audio length)

5. Verify output:
   ✓ Transcribed text appears
   ✓ Accuracy depends on audio quality


PHASE 1E: Create Windows Executable
──────────────────────────────────

1. Install PyInstaller:
   pip install pyinstaller

2. Create executable:
   pyinstaller --onefile --windowed --name "SpeechRecognition" main.py

3. Wait for compilation (takes 1-3 minutes)

4. Find executable:
   dist\SpeechRecognition.exe

5. Test the .exe:
   Double-click dist\SpeechRecognition.exe

6. Verify:
   ✓ App launches without terminal window
   ✓ All features work
   ✓ Can be shared with others


PHASE 1F: Distribute on Windows
───────────────────────────────

Option A: Portable Executable
   • Copy dist\SpeechRecognition.exe to anywhere
   • Share with others
   • No installation needed

Option B: Windows Installer
   • Use Inno Setup or NSIS
   • Creates .exe installer
   • Professional distribution

Option C: Windows Store
   • Submit to Microsoft Store
   • Requires developer account
   • Auto-updates

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📱 PART 2: ANDROID TESTING & DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 2A: Prerequisites
──────────────────────

1. Install Java Development Kit (JDK):
   • Download: https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html
   • Version: JDK 17 or newer
   • Install to default location

2. Install Android SDK:
   • Download: https://developer.android.com/studio
   • Android Studio
   • Install SDKs:
     - Android API 31 (Target)
     - Android API 34 (Latest)

3. Set environment variables:
   • JAVA_HOME = C:\Program Files\Java\jdk-17
   • ANDROID_HOME = C:\Users\<username>\AppData\Local\Android\sdk

4. Install Buildozer and dependencies:
   pip install buildozer cython

5. Verify installation:
   buildozer --version
   ✓ Should show version number


PHASE 2B: Build Android APK
────────────────────────────

1. In d:\Speech_Recognition directory:

2. Initialize buildozer (creates buildozer.spec):
   buildozer init

3. Edit buildozer.spec:
   [app]
   title = Speech Recognition
   package.name = speechrecognition
   package.domain = org.speechrec
   
   requirements = python3,kivy,vosk,numpy,matplotlib,requests,sounddevice,soundfile
   
   orientation = portrait
   fullscreen = 0
   
   android.permissions = RECORD_AUDIO,INTERNET,READ_EXTERNAL_STORAGE,WRITE_EXTERNAL_STORAGE
   android.api = 31
   android.minapi = 21
   android.accept_sdk_license = True

4. Build debug APK (first time = long!):
   buildozer android debug

   Wait time: 10-30 minutes (first time)
   Subsequent: 2-5 minutes

5. Find APK:
   bin\speechrecognition-1.0.0-debug.apk


PHASE 2C: Install on Android Phone
───────────────────────────────────

Option A: Via USB Cable
   1. Connect Android phone to Windows (USB cable)
   2. Enable USB Debugging on phone:
      Settings > Developer Options > USB Debugging
   
   3. In command line:
      adb install bin\speechrecognition-1.0.0-debug.apk
   
   4. Wait for installation to complete
   
   5. App appears on phone home screen

Option B: Manual Installation
   1. Copy APK to phone via USB
   2. On phone: Settings > Security > Allow unknown sources
   3. Open file manager
   4. Tap APK file
   5. Tap Install


PHASE 2D: Android Testing
─────────────────────────

1. Launch app on phone

2. Verify on screen:
   ✓ App name "Speech Recognition"
   ✓ 6 buttons visible (in portrait mode)
   ✓ Clean interface

3. Test Record button:
   ✓ Microphone permission popup
   ✓ Allow permission
   ✓ Recording works
   ✓ Audio saves to phone

4. Test other buttons:
   ✓ Upload (select audio from gallery)
   ✓ Transcribe (process audio)
   ✓ Visualize (view graphs)
   ✓ Settings (app settings)
   ✓ Exit (close app)

5. Test on different Android versions:
   ✓ Android 7 (API 21)
   ✓ Android 9 (API 28)
   ✓ Android 12 (API 31)
   ✓ Android 14 (API 34)


PHASE 2E: Optimize for Mobile
──────────────────────────────

Issue: APK size too large
Solution:
   1. Remove unused dependencies from requirements
   2. Use smaller Vosk model
   3. Compress assets
   
Result: APK should be 50-150MB

Issue: Vosk model missing
Solution:
   1. Use smaller model: vosk-model-small-en-us-0.15
   2. Or download model on first launch
   3. Store in app's cache directory


PHASE 2F: Release Android APK
──────────────────────────────

1. Build release APK:
   buildozer android release

2. Sign APK:
   jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 \
   -keystore my-release-key.keystore \
   bin\speechrecognition-1.0.0-release-unsigned.apk

3. Align APK:
   zipalign -v 4 \
   bin\speechrecognition-1.0.0-release-unsigned.apk \
   bin\speechrecognition-1.0.0-release.apk

4. Upload to Google Play Store:
   • Create Google Play Developer account
   • Upload APK
   • Add description, screenshots
   • Publish


PHASE 2G: Android Troubleshooting
──────────────────────────────────

Problem: "No audio input device found"
Solution:
   • Grant RECORD_AUDIO permission
   • Test with speaker first
   • Try different microphone apps

Problem: "Vosk model not found"
Solution:
   • Download manually before first use
   • Or use online transcription API

Problem: "App crashes on startup"
Solution:
   • Check logcat: adb logcat
   • Ensure all permissions granted
   • Update to latest Android version

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 PART 3: WEB DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 3A: Run FastAPI Server
────────────────────────────

1. Activate environment:
   .venv\Scripts\activate

2. Start server:
   uvicorn server:app --host 0.0.0.0 --port 8000

3. Output should show:
   Uvicorn running on http://127.0.0.1:8000
   Application startup complete

4. Access from browser:
   http://localhost:8000
   http://192.168.x.x:8000 (from other computer)


PHASE 3B: Create Web Interface
──────────────────────────────

1. Create static/ folder:
   mkdir static

2. Create static/index.html:
   <html>
   <head>
       <title>Speech Recognition</title>
       <style>
           body { font-family: Arial; max-width: 800px; margin: 50px auto; }
           .button { padding: 10px 20px; margin: 5px; cursor: pointer; }
           .results { border: 1px solid #ccc; padding: 10px; margin-top: 20px; }
       </style>
   </head>
   <body>
       <h1>Speech Recognition</h1>
       <div class="controls">
           <button class="button" onclick="record()">Record</button>
           <button class="button" onclick="upload()">Upload</button>
           <button class="button" onclick="transcribe()">Transcribe</button>
           <button class="button" onclick="visualize()">Visualize</button>
       </div>
       <div id="results" class="results"></div>
       <script src="app.js"></script>
   </body>
   </html>

3. Create static/app.js:
   function record() {
       fetch('/api/record', { method: 'POST' })
           .then(r => r.json())
           .then(d => document.getElementById('results').innerHTML = d.message);
   }
   
   function upload() {
       // File upload logic
   }
   
   function transcribe() {
       // Transcription logic
   }
   
   function visualize() {
       fetch('/api/visualize')
           .then(r => r.json())
           .then(d => showVisualization(d));
   }


PHASE 3C: Deploy to Local Network
──────────────────────────────────

1. Find your computer IP:
   ipconfig
   Look for: IPv4 Address (example: 192.168.1.100)

2. Access from other computer:
   http://192.168.1.100:8000

3. Test from phone on same WiFi:
   Open browser: http://192.168.1.100:8000

4. All features work from browser:
   ✓ Upload audio
   ✓ Transcribe
   ✓ View results


PHASE 3D: Deploy to Internet
─────────────────────────────

Option A: Heroku
   1. heroku login
   2. heroku create speech-recognition-app
   3. git push heroku main
   4. Access: https://speech-recognition-app.herokuapp.com

Option B: AWS
   1. Create EC2 instance
   2. Upload files
   3. Install dependencies
   4. Run: uvicorn server:app --host 0.0.0.0 --port 8000
   5. Access: http://ec2-instance-ip:8000

Option C: Google Cloud
   1. gcloud auth login
   2. Create app.yaml
   3. gcloud app deploy
   4. Access: https://project-id.appspot.com


PHASE 3E: Web Troubleshooting
──────────────────────────────

Problem: "Connection refused"
Solution:
   • Make sure server is running
   • Check port 8000 is open
   • Try localhost vs IP address

Problem: "Upload fails"
Solution:
   • Check file permissions
   • Verify disk space
   • Check upload directory exists

Problem: "Transcription timeout"
Solution:
   • Increase timeout in FastAPI settings
   • Run transcription in background task
   • Use async processing

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🐳 PART 4: DOCKER DEPLOYMENT (ALL PLATFORMS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 4A: Create Dockerfile
───────────────────────────

1. Create file: Dockerfile (no extension)

2. Add content:
   FROM python:3.12-slim
   
   WORKDIR /app
   
   RUN apt-get update && apt-get install -y \
       libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev \
       libsdl2-ttf-dev libportmidi-dev libswscale-dev \
       libavformat-dev libavcodec-dev zlib1g-dev
   
   COPY requirements.txt .
   RUN pip install --no-cache-dir -r requirements.txt
   
   COPY . .
   
   RUN python model_downloader.py
   
   EXPOSE 8000
   
   CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]


PHASE 4B: Build Docker Image
────────────────────────────

1. Install Docker:
   https://www.docker.com/products/docker-desktop

2. Build image:
   docker build -t speech-recognition .

3. Wait for build (2-5 minutes)

4. Verify image:
   docker images
   ✓ "speech-recognition" should appear


PHASE 4C: Test Docker Locally
─────────────────────────────

1. Run container:
   docker run -p 8000:8000 speech-recognition

2. Access in browser:
   http://localhost:8000

3. Test all features:
   ✓ Upload audio
   ✓ Transcribe
   ✓ Visualize
   ✓ Settings

4. Stop container:
   Ctrl+C


PHASE 4D: Deploy Docker to Cloud
─────────────────────────────────

Option A: Heroku
   1. heroku container:login
   2. docker tag speech-recognition registry.heroku.com/app-name/web
   3. docker push registry.heroku.com/app-name/web
   4. heroku container:release web -a app-name
   5. Access: https://app-name.herokuapp.com

Option B: AWS ECR
   1. aws ecr create-repository --repository-name speech-recognition
   2. docker tag speech-recognition:latest <account>.dkr.ecr.<region>.amazonaws.com/speech-recognition:latest
   3. docker push <account>.dkr.ecr.<region>.amazonaws.com/speech-recognition:latest
   4. Deploy to ECS or EKS

Option C: Google Cloud Run
   1. gcloud auth configure-docker
   2. docker tag speech-recognition gcr.io/project-id/speech-recognition
   3. docker push gcr.io/project-id/speech-recognition
   4. gcloud run deploy speech-recognition --image gcr.io/project-id/speech-recognition

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 TESTING MATRIX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FEATURE              Windows    Android    Web        Docker
─────────────────────────────────────────────────────────────
Recording            ✓          ✓          ⚠️          ⚠️
Transcription        ✓          ✓          ✓           ✓
File Upload          ✓          ✓          ✓           ✓
Visualization        ✓          ✓          ✓           ✓
Settings             ✓          ✓          ✓           ✓
Offline              ✓          ✓          ✗           ✗
Portable             ✓          ✓          ✓           ✓
Distribution         Easy       Medium     Easy        Hard

Legend: ✓ Full support, ⚠️ Partial/Configuration needed, ✗ Not supported

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 FINAL CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WINDOWS:
   ☐ App runs from source (python main.py)
   ☐ All 6 buttons functional
   ☐ Recording works
   ☐ Transcription works
   ☐ Executable created (PyInstaller)
   ☐ Executable runs on fresh Windows install

ANDROID:
   ☐ Prerequisites installed (JDK, Android SDK)
   ☐ APK builds successfully (buildozer)
   ☐ APK installs on phone
   ☐ All buttons work on mobile
   ☐ Recording works with proper permissions
   ☐ Vosk model installed
   ☐ Transcription works

WEB:
   ☐ FastAPI server runs
   ☐ Browser access works (localhost:8000)
   ☐ Upload functionality works
   ☐ Transcription works
   ☐ Visualization works
   ☐ Accessible from other computer on network

DOCKER:
   ☐ Dockerfile created
   ☐ Docker image builds
   ☐ Container runs locally
   ☐ Web interface accessible in container
   ☐ All features work in container
   ☐ Deployable to cloud

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 TIPS & TRICKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Testing:
   • Always test on clean system first
   • Use virtual machines for testing different OS
   • Test with different audio input devices

Performance:
   • Windows: Fastest (native)
   • Android: Medium (compiled)
   • Web: Depends on server
   • Docker: Same as native when optimized

Size Optimization:
   • Windows .exe: 50-100MB
   • Android APK: 80-150MB
   • Web: Minimal (server-based)
   • Docker: 500MB-1GB

Deployment Sequence:
   1. Start with Windows (easiest to debug)
   2. Move to Android (moderate difficulty)
   3. Try Web (good for testing backend)
   4. Docker last (for production/cloud)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ COMPLETION STAGES

   ✓ Stage 1: Windows testing complete
   → Stage 2: Create Windows .exe
   → Stage 3: Android APK creation
   → Stage 4: Mobile testing
   → Stage 5: Web deployment
   → Stage 6: Cloud deployment

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Good luck with your deployments! 🚀

For detailed info, see DEPLOYMENT_GUIDE.md
